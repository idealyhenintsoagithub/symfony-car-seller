{% extends 'admin/admin-layout.html.twig' %}

{% block title %}Detail fournisseur{% endblock %}

{% set actions = [
    { 
        'path': 'app_vendor_index', 
        'label': 'Liste des fournisseurs', 
        'iconClass': 'bi bi-list' 
    },
    {
        'path': 'app_vendor_edit',
        'label': 'Modifier',
        'iconClass': 'bi bi-pencil',
        'variables': vendor.id
    }
] %}

{% block body %}
    <div>
        <h1>Detail du produit</h1>
        {{ include("shared/page-actions.html.twig", { actions: actions }) }}
        <div class="p-2 bg-white">
            <table class="table p-2">
                <tbody>
                    <tr>
                        <th>Logo</th>
                        <td>
                            {{ include("shared/table-image.html.twig", { 'imagePath': '/images/vendors/' ~ vendor.logo }) }}
                        </td>
                    </tr>
                    <tr>
                        <th>No, du fournisseur</th>
                        <td>{{ vendor.name }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
    {# <a href="{{ path('app_vendor_index') }}">back to list</a>

    <a href="{{ path('app_vendor_edit', {'id': vendor.id}) }}">edit</a> #}

    {# {{ include('vendor/_delete_form.html.twig') }} #}
{% endblock %}
